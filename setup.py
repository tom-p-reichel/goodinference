from setuptools import setup



setup(
    name = "goodinference",
    version =  "0.0.1",
    author = "Thomas Reichel",
    description = "batteries included llm embedding loop",
    packages = ["goodinference" ]
)
